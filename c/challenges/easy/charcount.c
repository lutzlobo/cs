/*
Counting Characters in a String:
Write a function int count_chars(char str[]) that takes a string as input and returns the number of characters in the string. Don't forget that strings in C are null-terminated, so you should stop counting when you encounter a null character (\0).
*/

